These are MetaBot configuration files. Copy them to <starcraft>/bwapi-data/AI if not in tournament.

If in tournament, copy the files from this directory to <starcraft>/bwapi-data/read, change the
value of Configuration::INPUT_DIR in SCProjects/Data/Configuration.cpp to point to this directory
and rebuild the project.